sources:
  - type: maven
    path: server/pom.xml
    modules:
      - jar_artifact
      - integration_tests
  - type: golang
    path: go-client/go.mod
linkReplacements:
  - https://javalin.io/javalin|https://javalin.io/
version: 0.3.6
excludes:
  - regex: "E-PK-CORE-26: '.github/workflows/release_droid_.*.yml' exists but must not exist. Reason: Release-droid workflows are replaced by release.yml"
